<!DOCTYPE html>
<html>
  <head>
    <title>
      <%= content_for?(:title) ? "#{yield(:title)} | Beginner Bounties" : "Beginner Bounties" %>
    </title>
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <meta property="og:title" content="Beginner Bounties" />
    <meta property="og:type" content="website" />
    <meta property="og:description" content="Help Junior developers get hired by sharing small projects they can work on to build their resume with paid work." />
    <meta property="og:url" content="<%= root_url %>" />
    <meta property="og:image" content="<%= image_url("open_graph.png") %>" />

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <%= render "shared/navbar" %>

    <% if notice.present? %>
      <div class="max-w-7xl mx-auto py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-lg" id="notice"><%= notice %></div>
    <% end %>

    <main class="max-w-7xl mx-auto p-6 lg:px-8 prose">
      <%= yield %>
    </main>

    <footer>
      <%= render "shared/footer" %>
    </footer>
  </body>
</html>
